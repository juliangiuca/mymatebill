require 'spec_helper'

describe Dealing do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Dealing.create!(@valid_attributes)
  end
end
